syntax = "proto3";
package Greenfield;

message gRPCMessage {

    uint32 id = 1;
    uint32 port = 2;
    string message = 3;
    uint64 timestamp = 4;

}

service gRPCService {

    rpc grpc(stream gRPCMessage) returns (stream gRPCMessage);
}
